const searchSelector = require('./search-selector');
const checkoutSelector = require('./checkout-selector');

module.exports = selectors = {
    searchSelector: searchSelector,
    checkoutSelector: checkoutSelector,
}